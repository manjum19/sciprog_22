In Practical 06, we implement a function to perform matrix multiplication. It was also kept in a seperate C file from the from the main function which means we need to generate the intermediary compile files before running it
